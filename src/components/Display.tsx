import * as React from "react";

interface Props {
}

interface State {
}

export default class Display extends React.PureComponent<Props, State> {
    render() {
        return (
            <div>
                <input/>
            </div>
        )
    }
}
